---
title: "code-line-style"
subtitle: "Named visual treatments for code blocks in Quarto"
format:
  html:
    toc: true
    code-overflow: wrap
filters:
  - code-line-style
---

A Quarto extension that decorates code blocks with **named styles** —
diff, emphasis, custom — applied to **regions** (lines, columns, regex
tokens) across **steps** (revealjs fragments).

Diff is one preset. The engine doesn't know what a diff is.

## Install

```bash
quarto add EmilHvitfeldt/quarto-code-line-style
```

Then in your document's YAML:

```yaml
filters:
  - code-line-style
```

## Quick taste

```{.r code-line-style="add:2-3, rem:5"}
df |>
  filter(cyl == 6) |>
  group_by(gear) |>
  summarise(m = mean(mpg)) |>
  arrange(desc(m))
```

## Examples

Each embedded deck demonstrates one concept end-to-end. Click into a frame
and use → / ← to walk through progressive specs. Each title links to the
standalone page.

```{=html}
<style>
.cs-example { margin: 1rem 0 2rem; }
.cs-example h3 { margin-bottom: 0.25rem; }
.cs-example iframe {
  width: 100%;
  aspect-ratio: 16 / 9;
  border: 1px solid #ddd;
  border-radius: 4px;
}
.cs-example iframe.cs-tall { aspect-ratio: auto; height: 600px; }
</style>
```

### [diff](examples/diff.html) — the 5 built-in styles {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/diff.html" loading="lazy"></iframe>
```
:::

### [emphasis](examples/emphasis.html) — focus / dim / strong {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/emphasis.html" loading="lazy"></iframe>
```
:::

### [custom-styles](examples/custom-styles.html) — define your own {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/custom-styles.html" loading="lazy"></iframe>
```
:::

### [transitions](examples/transitions.html) — timing, modes, presence {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/transitions.html" loading="lazy"></iframe>
```
:::

### [annotations](examples/annotations.html) — margin labels {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/annotations.html" loading="lazy"></iframe>
```
:::

### [code-line-numbers](examples/code-line-numbers.html) — emulating the built-in {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/code-line-numbers.html" loading="lazy"></iframe>
```
:::

### [html-format](examples/html-format.html) — outside revealjs {.unnumbered}

::: {.cs-example}
```{=html}
<iframe src="examples/html-format.html" class="cs-tall" loading="lazy"></iframe>
```
:::

## Authoring forms

### Compact attribute (any code block)

```
{.r code-line-style="add:2-3, rem:5"}                # static
{.r code-line-style="|hi:2|rem:3,add:4-7|hi:8"}      # progressive
```

### YAML cell option (executable chunks)

```yaml
#| code-line-style:
#|   preset: emphasis
#|   styles:
#|     warn: { bg: "#fff3bf", gutter: "#f08c00", marker: "!" }
#|   transition: { duration: 400, mode: slide }
#|   steps:
#|     - focus: 3
#|     - warn: { line: 4, match: "bug", label: "this one" }
```

## Concepts

- **Style** — name → CSS class (preset) or inline primitives
  (`bg`, `fg`, `gutter`, `opacity`, `weight`, `marker`, `duration`, `easing`).
- **Region** — `at: 2-3`, `at: { line, label }`, or short keys: `add: 2-3`.
- **Step** — top-level list = reveal.js fragments. Single object = static.

## Accessibility

- Transitions wrap `@media (prefers-reduced-motion: reduce)`.
- Every style ships a gutter `marker` character so colorblind / monochrome
  viewers can still read the decoration.
- Step labels are announced via an `aria-live` region.

## Debugging

Append `?cls-debug=1` to any rendered URL to surface inline error banners
above blocks with malformed specs, unknown style names, or out-of-range
line numbers.
